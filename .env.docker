# Docker用環境変数設定ファイル
# このファイルを.envにコピーしてから docker-compose を実行してください
# 
# 使用方法:
# 1. このファイルを .env にコピー: cp .env.docker .env
# 2. 以下の値を実際の Azure AI Foundry Agent の設定に変更
# 3. docker-compose up -d で起動

# Azure AI Foundry Agent の設定
VITE_AZURE_AI_FOUNDARY_ENDPOINT_URL=https://your-agent-endpoint.azure.com/api/projects/your-project
VITE_AZURE_AI_FOUNDARY_API_KEY=your-api-key-here
VITE_AZURE_AI_AGENT_ID=your-agent-id-here
